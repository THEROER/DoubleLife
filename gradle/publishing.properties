# Required by the MagicUtils matrix-settings plugin (loadPublishingSpec), but
# DoubleLife is NOT published to a Maven repository — it ships as a plugin jar.
# These values are placeholders so the matrix plugin resolves; no Maven publish
# task is run for DoubleLife.
group=dev.ua.theroer
repo.url=https://maven.theroer.dev/releases
smoke.artifact=doublelife-bukkit
