package dev.ua.theroer.doublelife.config;

/**
 * A per-player preference that a player may override for themselves via
 * {@code /dl settings}, when the corresponding server setting allows it.
 */
public enum PlayerSetting {
    /** Whether the player is protected from hostile mobs in their second life. */
    MOB_PROTECTION
}
